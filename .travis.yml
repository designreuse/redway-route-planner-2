language: java
jdk:
  - oraclejdk8

services:
  - docker

before_script:
  - (cd src/main/docker && exec docker-compose up -d)
  - docker ps
  - sleep 30

install: mvn install -P docker,integration-test

after_script:
  - (cd src/main/docker && exec docker-compose down)