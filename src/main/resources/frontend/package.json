{
  "name": "redway-route-planner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "rimraf dist && npm run build",
    "build": "npm run static",
    "test": "echo \"Error: no test specified\" && exit 1",
    "static": "copyfiles -u 1 src/**/* ../static",
    "watch": "npm-watch"
  },
  "watch": {
    "build": "src/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^1.0.0",
    "npm-watch": "^0.1.7",
    "rimraf": "^2.5.4"
  }
}
