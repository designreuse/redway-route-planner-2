{
  "name": "redway-route-planner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "rimraf dist && npm run build",
    "build": "npm run static",
    "build-dev": "npm run build && npm run copy-to-target",
    "copy-to-target": "copyfiles -u 2 ../resources/static/**/* ../../../target/classes",
    "test": "echo \"Error: no test specified\" && exit 1",
    "static": "copyfiles -u 1 src/**/* src/* ../resources/static",
    "watch": "npm-watch"
  },
  "watch": {
    "build-dev": "src/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^1.0.0",
    "npm-watch": "^0.1.7",
    "rimraf": "^2.5.4"
  }
}
